{
  "name": "lendsure",
  "version": "1.0.0",
  "private": true,
  "description": "LendSure — AI lending intelligence. Vite + React frontend; FastAPI backend served from api/ (Python handlers) in production.",
  "scripts": {
    "dev": "sh ./scripts/preview.sh",
    "preview": "sh ./scripts/preview.sh",
    "build": "npm run build:host && npm run build:local",
    "build:host": "cd frontend && VERCEL=1 npx vite build --outDir ../dist --emptyOutDir",
    "build:local": "cd frontend && npx vite build"
  },
  "dependencies": {
    "@react-three/drei": "^10.7.8",
    "@react-three/fiber": "^9.7.0",
    "framer-motion": "^13.2.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router-dom": "^7.18.3",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2"
  }
}
